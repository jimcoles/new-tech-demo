/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'war'
}

war.archiveFileName = "demo-services.war"

dependencies {

    // No particular use pushing sub-project deps to top-level management section IMHO
    implementation( project(':services-core-ws') )
    implementation( project(':demo-shared-api') )
    implementation( project(':user-service-resteasy') )
    implementation( project(':userdb-service-resteasy-mongo') )

    // The following dep is defined in top-level build.gradle 'ext'. Just trying that out but there are issues.
    compile( libraries.javaWsRsApi )

    constraints {

    }
}

